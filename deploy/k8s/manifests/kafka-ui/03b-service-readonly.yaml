apiVersion: v1
kind: Service
metadata:
  name: kafka-ui-readonly
  namespace: kafka-ui
  labels:
    app.kubernetes.io/name: kafka-ui
    app.kubernetes.io/component: readonly
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kafka-ui
    app.kubernetes.io/component: readonly
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
